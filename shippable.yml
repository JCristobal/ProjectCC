# Configuración del lenguaje
language: python

# Version
python:
   - "2.6"
   - "2.7"

services:
   - mongodb


#Instalar requerimientos 
install:
   - pip install -r requirements.txt

script:
   - nosetests test.py
